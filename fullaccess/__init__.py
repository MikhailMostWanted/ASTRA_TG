"""Legacy Telegram access layer kept behind `astra_runtime` adapters.

This package is the temporary pre-pivot Telethon contour. Do not add new
product behavior here; expose new runtime capabilities through
`astra_runtime.contracts` and keep this package only as the legacy adapter
until the replacement runtime is ready.
"""
